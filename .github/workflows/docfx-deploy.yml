name: Deploy docfx content to Pages

on:
  push:
    branches: ["main"]
    paths: ["docs/**"]
  workflow_dispatch:

jobs:
  publish-docs:
    runs-on: ubuntu-20.04
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        submodules: true
    
    - name: Setup .NET 6
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '6.x'
        
    - name: Generate docs
      run: |
        git pull --recurse-submodules
        echo '{"sdk":{"version": "6.0.0","rollForward":"latestPatch"}}' > global.json
        dotnet restore src/DNVGL.SolutionPackage.sln        
        dotnet tool update -g docfx
        docfx docs/docfx.json 
        
    - name: publish gh-pages
      run: |
        cd docs/_site
        git config user.email "github-actions-bot@dnv.com"
        git config user.name "github actions boot"
        git add .
        git commit -m "Update docs" || echo "No change to commit"
        git push origin HEAD:gh-pages
